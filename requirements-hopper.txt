# Hopper Python deps (PyPI). Full env: sbatch scripts/setup_hopper_env.slurm
#
# Manual install (after loading modules — do NOT load ohpc):
#   module load hosts/hopper gnu12/12.3.0 cuda/12.6.2-ou python/3.11.7-cx
#   pip install torch==2.7.0 \
#     --index-url https://download.pytorch.org/whl/cu126 \
#     --extra-index-url https://pypi.org/simple
#   pip install -r requirements-hopper.txt
#   FLASH_ATTENTION_FORCE_BUILD=TRUE pip install flash-attn==2.8.3 --no-build-isolation

fsspec>=2023.1.0,<=2026.4.0
transformers>=4.48.0
tokenizers>=0.21.0
datasets>=5.0.0
hydra-core>=1.3.3
lightning>=2.4
wandb>=0.18
